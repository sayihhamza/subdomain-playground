# Subdomain Scanner - Environment Variables
# Copy this file to .env and customize paths as needed

# ===== Tool Binary Paths =====
# Override default paths for enumeration tools
# If not set, defaults to project bin/ or ~/go/bin/

# Subdomain enumeration tools
SUBFINDER_PATH=/path/to/subfinder
AMASS_PATH=/path/to/amass
FINDOMAIN_PATH=/path/to/findomain
PUREDNS_PATH=/path/to/puredns
ALTERX_PATH=/path/to/alterx

# Validation and detection tools
DNSX_PATH=/path/to/dnsx
HTTPX_PATH=/path/to/httpx
SUBZY_PATH=/path/to/subzy

# ===== Data Paths =====
# Override paths for wordlists and resolvers
WORDLIST_PATH=/path/to/wordlist.txt
RESOLVERS_PATH=/path/to/resolvers.txt

# ===== Scan Options =====
# Default values for common options
SCAN_WORKERS=5
SCAN_MODE=quick
OUTPUT_DIR=data/scans

# ===== Example Configurations =====

# Example 1: Use system-installed tools
# SUBFINDER_PATH=/usr/local/bin/subfinder
# AMASS_PATH=/usr/local/bin/amass
# HTTPX_PATH=/usr/local/bin/httpx

# Example 2: Use custom Go installation
# AMASS_PATH=/opt/go/bin/amass
# FINDOMAIN_PATH=/opt/go/bin/findomain

# Example 3: Use tools from different locations
# SUBFINDER_PATH=/home/user/tools/subfinder
# DNSX_PATH=/home/user/tools/dnsx
# HTTPX_PATH=/usr/bin/httpx

# ===== Usage =====
# 1. Copy this file: cp .env.example .env
# 2. Uncomment and set paths you want to override
# 3. Run scanner: python scan.py --collect-domains myleadfox --provider Shopify
#
# Note: Environment variables take precedence over default paths
